import SwiftUI

extension Color {
    static let userBubble = Color.blue
    static let assistantBubble = Color(.systemGray5)
    static let userText = Color.white
    static let assistantText = Color(.label)
    static let codeBackground = Color(.systemGray6)
}
